Dayflow HRMS Full Stack

Run the backend from the ../backend folder with npm install then npm start.
Then open http://localhost:3000/login/login.html
Do not use the old hard-coded login or Live Server for this full-stack version.
